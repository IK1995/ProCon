for(int i=0;i<26;i++) al[i]=0;//初期化

//アルファベットの使用判定
	for(int i=0;s[i]!='\0';i++){
		al[s[i]-'a']=1;//文字コードに直している（0～26）
	}
	
//使用していない辞書順最小の文字を出力
	for(int i=0;i<26;i++){
		if(al[i]==0){
			cout<<(char)(i+'a')<<endl;//文字コードから文字に変換
			return 0;
		}
	}